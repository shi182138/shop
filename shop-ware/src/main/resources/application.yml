spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://192.168.85.132:3306/shop_wms
    driver-class-name: com.mysql.cj.jdbc.Driver
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
  application:
    name: shop-ware
  jackson:
    date-format: YYYY-MM-dd HH:mm:ss
mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto
server:
  port: 11000

logging:
  level:
    com.example.shop: debug

